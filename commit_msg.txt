Améliorations majeures : extracteurs, streaming, saisons, recommandations

animesultra:
- loadStreams() réécrite : regex flexible (3 patterns), decodeHtmlEntities, fallbacks iframe/direct, dédup serveurs
- Myvi, Uqload, Verystream extracteurs ajoutés à resolveStream()
- load() : recommandations depuis le DOM + exclusion par ID numérique
- getHome() : fix extractFlwItem titre/URL séparés (poster link vs title link)
- Gestion des saisons améliorée (parseSeasonInfo, contentType hérité)
- Recommandations opérationnelles

animesama:
- Gestion des saisons refactorée type animesultra (regroupement par titre, same season VF+VOSTFR)
- Myvi, Uqload, Verystream extracteurs ajoutés
- data-src supporté dans search() et load()

animevostfr:
- Myvi, Uqload, Verystream extracteurs ajoutés
- load() : fixUrl sur le poster

frenchanime:
- Uqload, Verystream extracteurs ajoutés
- data-src prioritaire dans tout getHome() et search()

voiranime:
- Myvi, Uqload, Verystream extracteurs ajoutés

vostfree:
- Uqload, Verystream extracteurs ajoutés à resolveStream()
- data-src ajouté aux recommendations
