import { ExtractorApi, IExtractorLink } from '../core/extractor_api';
export declare class StreamTape extends ExtractorApi {
    name: string;
    mainUrl: string;
    requiresReferer: boolean;
    getUrl(url: string, referer?: string): Promise<IExtractorLink[]>;
}
